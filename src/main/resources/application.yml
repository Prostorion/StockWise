spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=stock_wise
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver
